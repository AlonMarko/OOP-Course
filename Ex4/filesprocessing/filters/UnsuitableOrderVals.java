package filesprocessing.filters;

import filesprocessing.TypeOne;

/**
 * unsuiutable orders exception
 */
public class UnsuitableOrderVals extends TypeOne {
    /**
     * constructor
     *
     * @param errorMessage a message to display when an error occurs.
     */
    public UnsuitableOrderVals(String errorMessage) {
        super(errorMessage);
    }
}
